---
import BackgroundImage from './BackgroundImage.astro';
import hero from '../assets/hero.jpeg';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class='hero'>
  <BackgroundImage image={hero} alt='' class:list={['image']} />

  <!--
  <div class='heading-container'>
    <h1 class='heading'>{title}</h1>
  </div>
  -->
</div>

<style>
  .hero {
    position: relative;
    display: flex;
    min-height: 279px;
    padding: clamp(12px, 4vw, 25px);
    background: gray;
    margin-bottom: 0;
  }

  /* .heading-container {
    position: relative;
    z-index: 2;
    display: flex;
    align-self: flex-end;
  }

  .heading {
    flex: 10;
    padding: 20px;
    font-size: clamp(1.4rem, 10vw, 2.25rem);
    font-style: italic;
    font-weight: 900;
    color: var(--orange);
    text-transform: uppercase;
    background: var(--color-heading-background);
  } */
</style>